
<%= simple_form_for(@cooperative) do |f| %>
  <% if @cooperative.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cooperative.errors.count, "error") %> prohibited this cooperative from being saved:</h2>

      <ul>
      <% @cooperative.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group"><%= f.input :name, class: "form-control"%></div>
  <div class="form-group"> <%= f.input :address, class: "form-control" %></div>
  <div class="form-group"><%= f.input :numTel, class: "form-control" %></div>
    
   
    
    <%= f.button :submit %>
<% end %>
